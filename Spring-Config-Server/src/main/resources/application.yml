spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shivam-ay/Spring-Cloud-Config
server:
  port: 8888
eureka:
  client:
    register:
      with:
        eureka: false